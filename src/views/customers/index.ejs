<%- include("../partials/header") %>

    <div class="container">
        <%- include("../partials/sidebar") %>

            <div class="main-content">
                <%- include("../partials/navbar") %>

                    <div class="content">

                        <div class="page-header">
                            <h1>Customers</h1>
                            <% if (permissions.includes('customer.create')) { %>
                                <a href="/customers/new" class="btn btn-primary">+ Add Customer</a>
                                <% } %>
                        </div>

                        <div class="table-container">
                            <table class="data-table">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Customer Code</th>
                                        <th>Name</th>
                                        <th>Mobile</th>
                                        <th>Email</th>
                                        <th>Address</th>
                                        <th>Status</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (typeof customers !=="undefined" && customers.length> 0) { %>
                                        <% customers.forEach(customer=> { %>
                                            <tr>
                                                <td>
                                                    <%= customer.id %>
                                                </td>
                                                <td><strong>
                                                        <%= customer.customer_code || "N/A" %>
                                                    </strong></td>
                                                <td>
                                                    <%= customer.name %>
                                                </td>
                                                <td>
                                                    <%= customer.mobile %>
                                                </td>
                                                <td>
                                                    <%= customer.email %>
                                                </td>
                                                <td>
                                                    <%= customer.address %>
                                                </td>
                                                <td>
                                                    <div class="status-container">
                                                        <span
                                                            class="status-text <%= customer.is_active !== false ? 'active' : 'inactive' %>">
                                                            <%= customer.is_active !==false ? 'Active' : 'Inactive' %>
                                                        </span>
                                                        <label class="switch">
                                                            <input type="checkbox" class="status-toggle"
                                                                data-id="<%= customer.id %>" <%=customer.is_active
                                                                !==false ? 'checked' : '' %>>
                                                            <span class="slider"></span>
                                                        </label>
                                                    </div>
                                                </td>
                                                <td class="actions">
                                                    <% if (permissions.includes('customer.edit')) { %>
                                                        <a href="/customers/<%= customer.id %>/edit"
                                                            class="btn btn-edit">Edit</a>
                                                        <% } %>
                                                            <% if (permissions.includes('customer.edit')) { %>
                                                                <form
                                                                    action="/customers/<%= customer.id %>?_method=DELETE"
                                                                    method="POST" style="display:inline;">
                                                                    <button type="submit" class="btn btn-delete"
                                                                        onclick="return confirm('Are you sure you want to delete this customer?')">Delete</button>
                                                                </form>
                                                                <% } %>
                                                </td>
                                            </tr>
                                            <% }) %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="8" class="empty-state">
                                                            No customers found. Add your first customer!
                                                        </td>
                                                    </tr>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>

                    </div>
            </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            document.querySelectorAll(".status-toggle").forEach(toggle => {
                toggle.addEventListener("change", async (e) => {
                    const id = e.target.getAttribute("data-id");
                    const isChecked = e.target.checked;
                    const statusText = e.target.closest(".status-container").querySelector(".status-text");

                    try {
                        const response = await fetch(`/customers/${id}/toggle-status`, {
                            method: "PATCH",
                            headers: { "Content-Type": "application/json" },
                            body: JSON.stringify({ is_active: isChecked })
                        });
                        const result = await response.json();
                        if (!result.success) {
                            alert("Error updating status: " + result.error);
                            e.target.checked = !isChecked;
                        } else {
                            // Update text and class color
                            if (isChecked) {
                                statusText.textContent = "Active";
                                statusText.className = "status-text active";
                            } else {
                                statusText.textContent = "Inactive";
                                statusText.className = "status-text inactive";
                            }
                        }
                    } catch (error) {
                        alert("Network error updating status.");
                        e.target.checked = !isChecked;
                    }
                });
            });
        });
    </script>


    <%- include("../partials/footer") %>